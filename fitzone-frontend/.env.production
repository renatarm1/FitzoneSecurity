# Usado por "npm run build". Vacío a propósito: el build se sirve desde el mismo
# dominio que expone la API de Django, así que las rutas relativas "/api/..." ya apuntan
# al lugar correcto sin necesidad de hardcodear un dominio aquí.
VITE_API_URL=
